To run this project you have to use an environment that has "node.js: ^14" and "npm" installed on it.

To start the project:
1- install node modules through running the command "npm install" or "yarn install" in the root directory of the project.
2- start the React server through the command "npm start".

You can access the application on the port 3000 with url: http://localhost:3000